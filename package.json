{
  "name": "@voiden/plugin-openapi-import",
  "version": "1.0.5",
  "type": "module",
  "private": false,
  "description": "Seamlessly migrate from OpenAPI by importing v3.0 collections and converting them to native Voiden request files with full support for nested folders, environment variables, headers, auth, and all request body types",
  "main": "./src/plugin.ts",
  "scripts": {
    "build": "node build.mjs",
    "test": "tsc --noCheck --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir .tmp-test test/mock-response.test.ts && node .tmp-test/test/mock-response.test.js",
    "release": "node build.mjs && node generate-manifest.mjs"
  },
  "peerDependencies": {
    "@voiden/sdk": "1.0.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@voiden/sdk": "1.0.10",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
